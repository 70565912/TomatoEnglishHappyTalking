# 分句整体框架重构实验（进行中，2026-08-15）

## 对照基线

- 当前最佳提交：Round 24 `4241b60`；
- 规则复核确认的不支持项：27 / 6792（0.398%）；
- 接纳条件：最终不支持项必须少于 27，且性能和代码结构不得回退；
- 实验分支可连续迭代，只有整体胜出后才一次性替换当前最佳。

## 检查点 1：递归 memo 改为迭代有界 DAG

- 保留同一候选事实、评分向量、工作集裁剪顺序和候选上限；
- 从结束词位向前计算，只访问起点和候选边界可达的状态；
- 没有 `requiredAfterWord` 时只计算单一状态；
- Alice E01/E21、Willows E30/E37 冻结回放的句子与原句路径完全一致；
- 本地分句回归 128 passed，1 个原生全量评测按环境开关跳过；
- Alice E21 五轮 AOT 交替中位数：Round 24 为 3012 ms，迭代 DAG 为 2953 ms，约快 1.96%；
- 求解器净增 5 行（39 增、34 删），消除了递归调用和字符串 memo key；
- 当前不支持项仍为 27 / 6792（0.398%），因此只作为框架分支内部检查点，不单独接纳。

## 已拒绝方向

Round 25 曾在旧稳定插入器外继续增加 21–24 词块补点逻辑，虽降至 23 / 6792，
但净增 76 行且没有性能收益，已留在独立失败分支，不并入本框架。

## 检查点 2：统一事实、迭代 DAG 与有界边界归一化

- 分支：`codex/splitter-framework-rebuild`；检查点 1 提交为 `b53b8b9`；
- 删除递归窗口枚举、重复的 pre-closure 候选事实和整窗局部重求解；同一正字句只建立一份候选事实，
  主路径使用自底向上的有界 DAG；
- 长度、结构风险、原文标点、引语/括号 span 和1词吸收继续走同一候选模型；
- 当前仍保留一个线性、有界的边界归一化步骤：只把已有缺陷切点迁到5词内的更强源标点或明确完整
  从句前缘，或在仍超过20词的稳定块增加一个边界；不递归、不重建事实、不重排整窗。它是下一阶段
  要吸收到统一 DAG 评分中的剩余架构债，因此本检查点不宣称已经满足规范第11、14节的最终形态；
- 101章冻结解析累计回放：Alice 39章、Willows 62章，共6909个输出句槽；1词、超过30词和28–30词
  均为0，最长27词；相对上一个冻结候选有164个同原句变化；
- 新增边界分类为：98个 `phraseComma`、19个 `clauseComma`、10个 `ambiguousComma`、31个
  `strongPunctuation`、39个 `dependencyClause`，没有新增 `emergency`。新增无标点边界的结构筛查只
  命中3个带表面告警的完整补语/关系从句前缘，逐项符合统一规范，没有成分内部断裂；
- 关键实书结果固定为：Alice E02 括号内部保持完整功能块；Willows E32 为
  `Breathless and transfixed, | the Mole ... wave, | caught ...`，同章分号句为
  `... near at hand; | and constantly ...`；关系从句从 `who/whom/whose/which/that` 前缘开始；
- 期间拒绝过“把任意附近无标点切点迁到标点”的过宽实现：它在 Willows E30 与跨正字句1词吸收组合
  下产生不可合并孤词。最终收窄为缺陷切点迁移；强标点只在同一稳定窗、两侧至少6词且不突破30词时
  优先，未保留该失败路径；
- 分句单元测试101项全部通过；聚焦静态分析4文件0问题。全项目分析仍有3个与本次无关的既有
  `unused_element` 告警；`git diff --check` 无新增空白错误；
- AOT 5轮交替中位数（Round 24 对本检查点）：Alice E21 `3,381,850 -> 2,673,617 us`
  （快20.9%），Willows E30 `5,819,080 -> 5,891,554 us`（慢1.2%），两项合计快6.9%；可执行文件
  `6,654,976 -> 6,650,880 bytes`；
- 求解器相对检查点1为410行新增、831行删除，净减421行；当前核心文件4,099行。结构与速度已经
  整体胜过 Round 24，但模块拆分和把有界归一化完全吸收进一次 DAG 尚未完成，所以只作为独立分支
  新基线，不合并生产分支，也不迁移文章、不重做TTS。

## 单 DAG 分支实验 1：删除结果后处理（未接纳）

- 分支：`codex/splitter-single-dag`，基于检查点 2；
- 同一正字句只构建一次候选事实；删除标点可达性独立探测、逐候选覆盖重求解、局部修复窗、稳定路径
  边界插入和结果后迁移；主路径由一次自底向上的有界 DAG 产生；
- 候选事实通过 `boundaryCandidates` 独立审计，不再要求低分候选为了“证明存在”进入前 24 条路径；
- 101 项固定回归通过，聚焦静态分析 0 问题；求解器由 4,099 行降至 3,519 行，相对检查点 2 为
  271 行新增、865 行删除，净减 594 行；
- 第一版纯通用路径全量冻结回放为 101/101 章变化，输出槽 `6909 -> 7023`，1 词和超过 30 词仍为 0，
  但这种扩散违反“保持现有最佳结果、只解决明确问题”的门槛，因此未进入不支持切法全量计数和性能
  接纳门禁；
- 随后把“通用路径、纯标点路径、20词内纯标点可达性”并入同一个 DAG 状态表，并增加路径内附近强
  标点优先评分。固定回归仍为 101/101，但 Alice E02/E09/E21、Willows E09/E30/E32/E37 七个代表
  章节仍全部与检查点 2 不同，其中 Willows E37 出现 `desperate | and dangerous fellow` 的修饰语内部
  断裂，代表集至少 1 个不支持切法，故本实验仍为失败检查点，不接纳、不做全书发布对比、不迁移文章
  或 TTS；
- 下一步必须把“完整成分边界”作为 DAG 边/相邻边对事实表达，消除上述代表集不支持切法，并把代表
  章节变化收敛到小范围后，才允许再次运行 Alice + Willows 全量门禁。

## 独立新框架的接纳方式

- 允许在 `codex/` 实验分支完全重构分句框架，不要求每一步都局部模拟旧实现；实验期间可连续迭代，
  但不得接入正式文章、数据库或 TTS；
- 新框架内部用固定单测和小型代表集缩短试错周期，只有代表集不支持项归零后才跑 Alice + Willows
  101 章全量门禁；
- 最终只做一次与当前最佳冻结结果的正式比较。接纳要求同时满足：不支持切法不增加、无 1 词句、
  无超过 30 词句、标点优先和 21–30 词分级压力不回退，并且速度、核心代码规模或模块清晰度至少一项
  有可测收益；任一质量硬门禁失败即保留为实验检查点，不替换现行实现；
- 全新实现仍必须是通用算法：不得出现书名、章节号、角色名、目标句原词白名单或结果后整窗重排。

## 单 DAG 分支实验 2：成分事实与全窗标点优先（未接纳）

- 分支：`codex/splitter-single-dag-components`；基于失败实验 1 继续迭代；
- 修正了候选提升会无差别删除结构告警的问题；明确不可在普通无标点边或 emergency 边切开限定词—
  中心词、修饰语—中心词、并列修饰语、极短并列名词尾巴和解析器误标形成的混合并列链；完整并列
  名词尾部仍允许由长度与完整性综合决定，因此 Alice E09 的 `birds | and animals that...` 保持通过；
- 同一 DAG 的评分把同一可行窗内合规源标点置于无标点边之前，不再限定为只看 5 词邻域。代表集
  Alice E02/E09/E21、Willows E09/E30/E32/E37 共 442 个输出句，不支持项 `0 / 442`，1 词和超过
  30 词均为 0，21–30 词 4 句；
- 101 项固定回归通过，聚焦静态分析 0 问题；核心求解器 3,661 行，比检查点 2 的 4,099 行少 438 行；
  AOT 回放程序 6,607,872 bytes，比检查点 2 的 6,650,880 bytes 少 43,008 bytes；
- 但 101 章全量门禁失败：101/101 章、767 个原句发生变化，输出槽 `6909 -> 7081`，21–30 词
  `26 -> 37`，最长 `27 -> 30`，新增 114 个无标点边界。规则复核确认至少 6 个不支持切法：
  `large | as the Rabbit`、`off | all its feet`、`had | but to open`、`early hour | that morning`、
  `idea or two | for his coming speeches`、`got | whispering`，即 `6 / 7081 = 0.085%`；
- 旧版影响门禁把 101 章全部判为包含 unexpected 变化；因此本实验即使代表集、代码行数和二进制体积
  有改善，也因全量质量扩散而不接纳，不运行文章迁移、不重做 TTS、不替换当前最佳冻结结果。

## 单 DAG 分支实验 3：通用附着保护与事实模块拆分（中间检查点）

- 分支：`codex/splitter-single-dag-attachments`；基于实验 2 的全量结果继续收敛，不改数据库、文章或
  TTS，也不与当前最佳分支合并；
- 用通用 UD/表面结构补齐六类附着事实：对称比较连接、`VERB + but + to + VERB` 固定连接、介词与
  右操作数、名词后置同位修饰、名词内部介词附着、无显式连词的并列谓词；没有书名、章节号、角色名
  或目标原词白名单；
- 将稳定的 UD 关系集合、附着词类和告警集合拆到 `read_aloud_splitter_v3_facts.dart`。主求解器由
  3,661 行降到 3,470 行；主求解器加事实模块共 3,585 行，仍比上一轮少 76 行；
- 101 项固定回归全部通过，聚焦静态分析 0 问题；扩大的 13 章代表集共 856 句，1 词句和超过 30 词
  句均为 0，六个旧坏切点的原字符串均消失；
- 101 章全量共 7,075 个输出句槽，1 词句 0，超过 30 词 0，最长 30 词，emergency 12；21--30 词
  为 42 句，高于实验 2 的 37 句。相对实验 2 有 37 个正字句的路径发生变化；
- 规则复核不能把六个字符串消失等同于六个问题解决：Willows E61 由
  `idea or two | for his coming speeches` 迁成同样不合规的 `idea | or two for his coming speeches`；
  另新增 `packed | and strapped`、`bells | ringing` 两个成分内部切点。首次记录曾按压缩上下文把
  `must jump | and hide` 和 `assigned to him | without a murmur` 误读为 `must | jump`、`to | him`；
  回查实际输出段后确认，前者是规则允许的共享主语并列谓词，后者是完整宾语后接状语，不计缺陷。
  因而本轮勘误为 `3 / 7075 = 0.042%` 个不支持切点，严格少于上一轮
  `6 / 7081 = 0.085%`，可保存为下一轮基线，但仍不接纳为现有最佳；
- AOT 回放器为 6,625,792 bytes，比实验 2 增加 17,920 bytes。代码规模改善，但舒适长度和二进制
  体积回退；下一轮必须在不支持项少于 3 的硬门槛下继续收敛，并优先把上述三种结构表达为候选事实，
  而不是增加结果后补丁。

## 单 DAG 分支实验 4：候选边的完整成分事实（中间检查点）

- 分支：`codex/splitter-single-dag-edge-facts`；基于勘误后的实验 3 基线 `3 / 7075` 继续，接纳硬门槛
  为全量不支持项少于 3；
- 在同一候选事实构建阶段阻止三类成分内部边：被并列中心词共享的前置补语、无显式关系词的分词
  后置修饰、名词与其并列数词。没有增加书籍/章节/目标原词白名单，也没有结果后修复窗；
- 目标正字句改为：`packed and strapped ... he saw | a plate...`（谓语—完整宾语）、
  `... tunnel they had so lately travelled | was borne...`（主语—谓语）、以及使用原文逗号和从句前缘
  保持 `idea or two` 完整；合法对照 `must jump | and hide`、`assigned to him | without...` 和
  `got whispering...` 均未改变；
- 101 项固定回归通过，聚焦静态分析 0 问题。101 章共 7,071 个输出句槽，1 词句 0，超过 30 词 0，
  最长 30 词；相对实验 3 只有 18 个正字句改变；
- 全量复核仍有 `the | pleasure`、`horrid | thin little laughs` 两个不支持切点，即
  `2 / 7071 = 0.028%`，严格少于实验 3 的 `3 / 7075 = 0.042%`，因此保存为下一轮基线，但仍不接纳；
- 代价是 21--30 词句 `42 -> 49`、emergency `12 -> 15`；主求解器与事实模块合计 3,632 行，较实验 3
  增 47 行；AOT 回放器 6,627,840 bytes，增 2,048 bytes。下一轮不支持项必须少于 2，并应同时收回
  这些副指标回退，不能仅靠继续增加阻断规则得到表面归零。

## 单 DAG 分支实验 5：边界排序归零（中间检查点）

- 分支：`codex/splitter-single-dag-edge-rank`；基于实验 4 的 `2 / 7071` 继续，不接入数据库、文章或
  TTS，也不替换现有生产最佳版本；
- 用候选边事实修复两类通用问题：把完整零关系词定语从句的左缘提升为合法成分边界；对 UDPipe 把
  连续名词修饰链误标成并列名词、并把后续介词短语挂到形容词的情况，禁止在修饰链内部切分。随后
  将无标点边界紧跟 `and/or/nor/but` 的右操作数保护收敛为一个与右侧误标词性无关的通用条件；没有
  书名、章节号、角色名、目标句原词白名单或结果后整窗补丁；
- 101 章全量最终为 7,074 个句槽，不支持切法 `0 / 7074 = 0.000%`，1 词句 0，超过 30 词句 0，
  最长 30 词；21--30 词 46 句，较实验 4 的 49 句减少 3；emergency 14，较实验 4 的 15 减少 1。
  相对实验 4，最终只有 5 个章节路径受协调词保护收敛影响，复核均为原文标点或合法的完整成分边界；
- 固定回归 101/101 通过，三个分句文件聚焦静态分析 0 问题。主求解器 3,535 行，事实模块 119 行，
  合计 3,654 行；相对完整重构前 4,099 行少 445 行（10.9%）。AOT 回放器 6,644,736 bytes，较冻结
  生产对照 6,650,880 bytes 小 6,144 bytes；
- 同机、同冻结解析输入、AOT 交替顺序运行 3 次取中位数：Alice E21 从 2,769,796 us 降到
  881,260 us，快 68.2%；Willows E30 从 6,142,589 us 降到 2,552,073 us，快 58.5%。并行全量回放
  的总耗时不用于性能结论；原始样本保存在 `output/sentence-split-v3/bench-*.json`；
- 本轮拒绝了两种“为了压低长句数而提前长度罚分”的尝试：第一种会跨过引号内句号合并句子，第二种
  会产生 `for a | minute`、`dare | do`、`were | not` 等成分内部切点；也拒绝了全局终止标点硬罚分，
  因其显著扩大求解状态并使 Willows E17 单章约耗时 17.8 秒。三种失败改动均已撤回；
- 结论：这是新框架内部可接受的“零不支持项”检查点，但不是生产替换结论。与冻结生产最佳结果相比，
  101/101 章仍发生路径变化，而且 21--30 词残留与最长块分布尚未全面胜出；因此保持独立分支，不迁移
  已保存文章、不重做 TTS。后续轮次必须保持不支持项为 0，并在不破坏标点优先的前提下继续改善舒适
  长度分布或其它次级指标；达到整体胜出后才进行一次正式生产替换对比。

## 单 DAG 分支实验 6：扩大每状态路径宽度（失败并撤回）

- 分支：`codex/splitter-single-dag-comfort`；假设 4 个 28--30 词残留块是每个 DAG 状态只保留 24 条
  路径导致的过早剪枝，临时把 `maxExpandedCandidatePaths` 从 24 提到 32；没有新增句法规则；
- Alice E15、Alice E35、Willows E42 三个目标章节的句槽数、`>20` 数、`>=28` 数和最长块全部不变，
  即 4 个目标长块一个也没有改善；单次同输入 AOT 回放耗时分别由 1,737,989 / 2,768,684 /
  2,547,466 us 增至 2,565,920 / 3,803,802 / 3,574,410 us，约慢 37%--48%；
- 结论：长块不是路径宽度不足造成。该参数已恢复为 24，不跑无收益的两书全量门禁；分支只保留本条
  失败记录，避免以后重复用扩大搜索宽度换取不存在的质量收益。

## 单 DAG 分支实验 7：标点无条件忽略物主先行词告警（失败并撤回）

- 分支：`codex/splitter-single-dag-comfort`；诊断发现 Willows E42 的原文逗号
  `hitherto, | his Mountains...` 被弱表面告警 `surface_possible_antecedent_possessive_separation` 压过，
  因此临时让所有标点边界忽略该告警；
- 12章代表集没有不支持项，促使本候选进入101章全量门禁。全量 `>20` 从 46 降到 36，`>=28` 从 4
  降到 2，最长从 30 降到 29；但共扩散到21章、23个正字句，emergency 从 14 增至 15；
- 全量复核发现该放宽会跨过更强终止标点，产生 `my pangs have been extreme. But couldn't...`、
  `Aren't you ashamed of yourself? What do...` 等合并，还会把仅12词的
  `He was not swift of foot; | his figure...` 过度切分，并移动引语闭缘；不支持项高于最近一次的 0；
- 结论：弱物主先行词告警确有误报，但不能在所有标点上无条件删除。实现已恢复到实验 5 的零不支持项
  检查点；下一轮只能在终止标点、完整成分和短句门禁之后，让 28--30 词压力有限地压过这种软告警。

## 单 DAG 分支实验 8：全部结构告警后移到极长句压力之后（失败并撤回）

- 分支：`codex/splitter-single-dag-critical-rank`；只把评分向量中的 `criticalLengthCount` 移到
  `structuralWarningRisk` 之前，终止标点缺失、不可读短块、不完整成分等更高门禁保持不变；
- 9章目标/反例集把全部4个 28--30 词残留降为0，最长降到27；Willows E38、E42 的新逗号路径可
  接受，但 Alice E15 同时产生 `off all its feet | at once`，切开完整介词附着，不支持项高于最近的0；
- 结论：28--30词压力可以压过误报的弱物主先行词告警，但不能压过介词、修饰语、并列成分等全部结构
  告警。评分次序已经恢复；下一轮应在事实评分中把弱物主先行词告警单独分级，不再移动整个结构层。

## 单 DAG 分支实验 9：标点物主告警分级（接受为分支检查点）

- 分支：`codex/splitter-single-dag-warning-tiers`；保留
  `surface_possible_antecedent_possessive_separation` 事实，但只在原文标点边界上把它计入独立的弱附着
  风险；介词、修饰语、并列成分等其它结构告警仍位于长度压力之前。评分顺序为：完整性与强结构风险、
  28--30词压力、弱标点物主风险、25--27词压力；没有删除告警、扩大搜索或增加个例词表；
- 相对实验 5 的零基线，101章全量只改变 Willows E38、E42 两个正字句。E38 将同一并列被动项的
  合法逗号停顿由 `caught, | reviled...` 移到更均衡的 `chains, | dragged...`（16/20 变 20/16）；E42
  把无标点 30/29 改为作者原文标点 20/20/19：`river, | and lay...hitherto, | his Mountains...`；
- 全量共 7,075 个句槽，不支持切法 `0 / 7075 = 0.000%`，1词句0，超过30词0，emergency保持14；
  21--30词由46降到44，28--30词由4降到2，最长由30降到29。固定回归101/101通过，聚焦静态分析
  0问题；
- 主求解器3,542行、事实模块119行，合计3,661行，较实验5增加7行，仍比重构前4,099行少438行；
  AOT回放器6,644,736 bytes，与实验5完全相同；
- 同输入 AOT 交替基准：Willows E30 三次中位数 2,447,956 -> 2,389,039 us（快2.4%）；Alice E21
  初次三次中位数波动较大，追加7次交替后为 755,680 -> 774,174 us（慢2.4%），但均值
  777,696 -> 777,699 us，差3 us，判定为噪声而非显著回退；
- 本轮在保持不支持项为0、代码仅小增且性能无显著回退的条件下，严格改善多个朗读长度指标，接受为
  新框架下一轮基线。仍不替换生产最佳、不迁移文章或TTS；剩余两个28/29词 Alice 块需按各自真实
  告警证据继续处理，不能扩大本轮弱告警范围。

## 单 DAG 分支实验 10：嵌套引号边缘取消归因尾告警（失败并撤回）

- 分支：`codex/splitter-single-dag-quote-edge`；假设 Alice E35 的28词尾块来自嵌套/错配引号边缘被
  误判为 attribution-only tail，临时规定仍位于引语内的 `after_closing` 边缘不生成该告警；
- Alice E15/E28/E35/E38、Willows E44/E55/E59 七章的句槽、长句指标和全部输出均完全不变；
- 结论：该告警不是目标路径缺失的决定因素，条件已经撤回，不跑无变化的全量门禁。候选诊断显示
  E35 前24条路径评分相同、尾部均为同一个28词块，只在前部路径变化，后续转查 DAG beam 的同分
  路径挤占问题。

## 单 DAG 分支实验 11：每状态同分路径限额（失败并撤回）

- 分支：`codex/splitter-single-dag-score-dedup`；在每个 DAG 状态排序后临时限制同一评分向量最多保留
  两条路径，使24条容量优先覆盖不同评分解，未增加候选或搜索宽度；
- Alice E15/E35 的句槽、`>20`、`>=28`、最长块和全部输出完全不变，目标29/28词块仍原样保留；
- 结论：同分路径挤占不是目标尾部方案缺失的根因，去重辅助代码已删除，不跑无变化的全量门禁。
  下一步直接诊断目标起点的后缀状态，找出候选被淘汰的确切阶段。

## 单 DAG 分支诊断 12：目标后缀状态取证（诊断代码已移除）

- 分支：`codex/splitter-dag-state-diagnostic`；用编译期常量临时输出每个 DAG 状态的前4条后缀路径，
  诊断结束后已删除打印，不进入正式代码；
- E15 起点20的29词块若在词33的逗号切入，需要的后缀方案已在起点33被局部自然度排序压下；这不是
  简单扩大宽度或同分去重可以解决，后续需要处理非加性评分的状态表达；
- E35 起点301的28词尾块中，`315/329` 的14/14强标点方案实际是该状态第2名；它唯一落后于 KEEP
  的高优先风险是 `quote_edge_attribution_only_tail`。右侧实际含 `I wish...`、`thought`、`hand...`
  多个谓词，并非单纯归因尾，说明 `_isAttributionOnlyRightTail` 把从句谓词排除后只数主谓词的定义过宽；
- 下一轮先用“右尾只允许一个实义谓词”的通用事实修正 E35；不得使用目标文本或 reporting verb 词表。

## 单 DAG 分支实验 13：归因尾只允许一个实义谓词（失败并撤回）

- 分支：`codex/splitter-single-dag-attribution-tail`；把 attribution-only tail 收紧为右侧全部实义动词
  只能有一个，且主语必须依附该谓词；没有 reporting verb 词表；
- 8章目标/反例集将 E35 的28词尾块切成14/14，但同时拆开多个短引语归因：
  `"Look at that!" | he cried...`、`"Give your evidence, | "said the King`，并在同章移动另外两处短引语；
- 结论：归因尾事实不能仅按谓词总数收紧，代码已恢复。告警应继续保护短引语，但可像标点物主告警
  一样降为弱风险，只允许28--30词压力压过，不能对所有长度无条件取消。

## 单 DAG 分支实验 14：归因尾告警分级（接受为分支检查点）

- 分支：`codex/splitter-single-dag-quote-warning-tier`；不改变归因尾事实，只把原文标点上的
  `quote_edge_attribution_only_tail` 与物主先行词告警放在同一弱结构风险层：短句仍优先保持引语与归因，
  只有28--30词压力可以压过；实现用两个直接字符串比较，避免评分热路径的小集合查找；
- 相对实验9，101章全量只改变 Alice E35 一个正字句：28词尾块在原文破折号/引号边缘变为14/14，
  `they looked...them—" | I wish...refreshments.`；短引语归因反例全部不变；
- 全量共7,076个句槽，不支持切法 `0 / 7076 = 0.000%`，1词句0，超过30词0，emergency保持14；
  21--30词由44降到43，28--30词由2降到1，最长保持29。固定回归101/101通过，聚焦静态分析0问题；
- 主求解器3,543行、事实模块119行，合计3,662行，仅比实验9增加1行；AOT回放器6,644,736 bytes，
  与实验9相同；
- 等价直接比较版本在 Alice E21 七次交替基准中，中位数 770,693 -> 770,118 us，均值
  788,223 -> 780,378 us，未见性能回退；此前 Willows E30 五次样本中候选亦未回退；
- 本轮保持零不支持项，并以1行净增换取唯一28词块消除，接受为新框架下一轮基线。剩余唯一29词块为
  Alice E15；其根因是非加性评分在后缀状态优先保留5词标点段，需从 DAG 状态表达处理，不能继续放宽
  结构告警。

## 单 DAG 分支实验 15：全面首出边多样性（失败并撤回）

- 分支：`codex/splitter-single-dag-first-edge-diversity`；每个 DAG 状态排序后先各保留一条不同首出边，
  再按原顺序补满24条，试图让局部次优但全局更优的后缀进入上游；搜索宽度不变；
- Alice E15 的唯一29词块消失，目标章最长降到20，但12章代表集已有6个正字句变化，并出现
  `if...do it! Oh dear!` 跨终止标点合并、`Soo— | oop!` 词内切分、
  `Constantinople, | friend? A fair...` 等不支持变化；不支持项高于最近的0；
- 结论：全面优先首出边多样性扩散过宽，代码已删除。下一轮只处理 top-24 全部共用同一首出边的
  beam 退化状态：保留原前23名，最多用最后1个槽位保存最佳不同首出边。

## 单 DAG 分支实验 16：单槽首出边逃逸（失败并撤回）

- 分支：`codex/splitter-single-dag-beam-escape`；只有一个状态的前24条路径全部共用同一首出边时，
  才用第24个槽保存最佳不同首出边，其余前23条和搜索宽度不变；
- Alice E15/E35 的输出与实验15完全相同，包括实验15已确认的终止标点合并和拟声词内部切分；说明
  只要不受约束的替代首出边进入上游，一个槽位也足以造成同样扩散；
- 结论：无条件单槽逃逸仍失败，代码已删除。下一轮只允许修复“共同首段不足8词”的退化状态，并要求
  替代首段达到至少8词；其它 beam 状态必须保持原样。

## 单 DAG 分支实验 17：舒适首段逃逸（失败并撤回）

- 分支：`codex/splitter-single-dag-short-prefix-escape`；在工作集和最终每状态路径剪枝时，若当前保留
  路径均没有 8--20 词首段，则用最后一个槽位保留首段落入 8--20 词的最佳后备路径；搜索宽度不变，
  也没有增加文本或章节特例；
- Alice E15 的唯一 29 词块消失，目标章最长降到 20；但同一正字句产生
  `rushed at the | stick`，切开谓词和完整宾语。Alice E35 还把拼写化拟声词再次切成
  `Soo— | oop!`；两者都是明确不支持边界，因此无需扩展到 101 章全量门禁；
- 结论：按首段长度给 beam 候选开逃逸槽仍会让局部非法路径进入全局解，不能作为通用修复。实现已
  删除，恢复实验 14 的零不支持项检查点。E15 的非加性状态问题不再继续叠加 beam 补丁；下一阶段
  应在独立分支按统一规范重构状态和目标函数，分支内连续优化，只有质量、性能和结构整体胜出后，
  才与生产最佳版本做一次性全量替换对比。

## Clean-room V4 阶段 1：可加 DAG 原型（隔离实验，尚未成为基线）

- 分支：`codex/splitter-clean-room-v4`。新实现位于 `app/tool/splitter_v4/`，只由冻结回放工具的显式
  `--experimental-additive` 参数调用；未接入产品 Service、Bridge 或发布产物，也未改变 V3 首选路径；
- 第一阶段只替换路径求解：把评分拆成区间代价与闭缘边界代价，在词位置 DAG 上精确保留 top-K 后缀。
  由于代价可加，不再依赖整路径反复评分、经验 beam 逃逸或目标章节补丁。聚焦测试覆盖 16 词 KEEP、
  23 词弹性切/不切、标点优先、强终止标点、弱标点告警、完整短功能块和重复字母延音，共 13 项通过；
- Alice E15 的 29 词残留降为最长 19，且没有再次出现 `rushed at the | stick`；E35 的词内
  `Soo— | oop` / `e— | e` 和跨 `song. "What...` 合并均消失。V4 单独路径求解在 E15/E35 一次样本约
  0.47/0.56 秒，但回放总耗时仍包含 V3 事实构建与旧求解，不能作为最终性能对比；
- 14章代表集第二次迭代覆盖354个正字句。Alice E02、E09、E11及 Willows E32、E37 的多个已批准
  标点/句法边界已出现，但仍发现 `she had | never before seen`、`stopped | rowing` 和 E37 20词尾段
  过度次切分，故本阶段不支持项大于最近基线的0，不接受为新检查点，也不运行两书全量；
- 根因：V4 当前临时消费 V3 暴露的原始 `boundaryCandidates`；其中信息性提示、强结构风险和待提升的
  延迟候选仍混在字符串 reasons/warnings 中，且个别助动词、动名词补语缺少应有事实。下一阶段先建立
  类型化、一次构建的 `BoundaryFacts`/`SegmentFacts` 适配层，再继续统一 DAG；不得用句子词面或章节
  特例修补上述三处。
- 已先完成无行为变化的职责拆分：`source_words.dart` 只构造词格与词内延音禁区，
  `boundary_facts.dart` 只把兼容输入归一化为强/弱结构风险，`additive_lattice.dart` 只做区间评分和 DAG。
  主求解模块由410行降为266行；三模块合计450行，增加的是类型接口而非重复求解。13项测试、聚焦分析
  和 Alice E02 冻结回放均通过，模块拆分前后 V4 输出逐字相同。

## Clean-room V4 阶段 2：直接依存紧密关系叠加（失败并撤回）

- 假设 V3 原始候选漏掉 `had | never seen`、`stopped | rowing` 的紧密谓词事实，因此临时把冻结
  `DependencyDocument` 切成正字句相对区间，在 V4 事实层叠加 UD 紧密关系 crossing、通用
  `VERB/AUX + ADV/PART/AUX* + VERB/AUX` 谓词链，以及 `cc:preconj -> conj/cc` 关联并列保护；没有
  使用目标词、章节或完整句子；
- 聚焦输入中三处目标错误均消失：`had | never seen`、`stopped | rowing` 和
  `either ... | or ...`；17项临时测试通过。但同一14章、1,022句槽代表门禁至少确认5处不支持切法：
  Willows E32 两处跳过更优原文逗号，Willows E37 出现 `to which, | of course, he had...`、
  `which, | though thoroughly effective...`，且上一轮的 `matter | and left...` 过度次切分仍存在；
- 本轮下限为 `>=5 / 1022 = >=0.489%`，高于最近候选的
  `>=3 / 1022 = >=0.294%`，违反“不支持项必须严格减少”门禁，判定失败。直接依存、POS谓词链、
  关联并列代码及临时测试已全部撤回，只保留本记录；不能再沿“给所有 crossing 统一加风险”的路径
  重试。后续 V4 事实必须直接产出每个间隙的类型化成分内/成分间角色，而不是在旧候选上叠加关系数量。

## Clean-room V4 阶段 3：共享谓语统一弹性风险（失败并撤回）

- 分支：`codex/splitter-clean-room-v4-flexible-syntax`；为所有带
  `deferred_stable_shared_predicate` 的无标点边界增加同一层弹性风险，使25词以上长度压力可压过，
  21--24词则允许 KEEP。该维度仍为可加边界代价，没有新增搜索或文本特例；
- 首次比较误用了残余风险排序调整前的旧代表报告，发现后废弃该比较，重新从父提交编译 AOT 基线，
  用同一14章、同一冻结输入比较。真实影响只有5个正字句：E37 从10/7/15/6改善为原文逗号后的
  10词块加完整主语/谓语17/10，E32 的时间从句入口也改善；但同时产生
  `swung the car round the yard | and out through...` 和 `hear that sound | once more...` 两个新不支持切点；
- 候选虽消除E37一处旧问题，但新增2处，已确认不支持下限由父版本
  `>=3 / 1022 = >=0.294%` 上升为 `>=4 / 1022 = >=0.391%`，判定失败。代码和两项临时测试已撤回。
  结论是“shared predicate”本身不足以决定21--24词KEEP；后续事实必须同时描述左右完整成分角色，
  不能继续给某一 reason 全局升降级。

## Clean-room V4 阶段 4：一次组件事实与标点优先（接受为分支检查点）

- 分支：`codex/splitter-clean-room-v4-component-facts`。本轮没有把书名、章节、目标词或完整句子写入
  决策；依存组件事实按整章一次构建为绝对字符边界索引，原句求解只做 O(1) 查表，DAG 仍只运行
  一次。`dependency_component_risks.dart` 负责 parser token 到组件内风险，`boundary_facts.dart` 只
  负责兼容候选归一化，`additive_lattice.dart` 只评分和求解；未接入产品 Service 或发布产物。
- 新增三类通用事实/顺序：`VERB/AUX + ADV/PART/AUX* + VERB/AUX` 的谓词链内部不切；
  `cc:preconj` 配对以及 `and/or + 前置修饰语 + 非谓语中心词` 的右操作数内部不切；无标点闭缘若
  跨过本段已有合格原文标点计罚，且“跳过原文标点”先于标点处的弱句法告警。长原句开头恰为3词、
  由逗号闭合且不在引语/括号内时，允许作为短导入停顿；2词前缀和不超过16词原句仍由短块门禁保护。
- 过宽中间版本全部被否决：把所有2--3词开头标点放行曾改变23个正字句并拆出短引语；把所有
  非谓语并列统一保护曾错误挡住 Alice E09 的 `birds | and animals that...`。最终只保护 `and` 后
  仍有前置限定/修饰语、尚未到达并列中心词的间隙；`and animals`、`and bound` 等直接右操作数保持
  可选，避免再次用统一词性规则扩散。
- 同一14章代表集相对父检查点仅8个正字句变化，逐项自行复核均支持：Alice E02 消除
  `had | never before seen`；Willows E32 得到已要求的 `Breathless and transfixed, | ... wave, |
  caught...`；另6处均为从无标点成分割裂移到原文标点，或把完整17词原句保留。典型改善包括
  `day, | its gurglings...`（11/10/16）、`Mountains of the Moon, | his limit...`（9/14），以及删除
  `diffident | and delaying`、`unexpected | and near`、`face ... | that had...`。
- 已确认不支持下限从 `>=3 / 1022 = >=0.294%` 降为
  `>=1 / 1022 = >=0.098%`；唯一已知残留是 Willows E37 的
  `...completed the matter | and left little further to discuss`，即 10/7/15/6 中末尾6词过度次切分。
  本轮严格改善但尚未达到0，因此只接受为 clean-room 分支新检查点，不与生产最佳做全量替换比较。
- 聚焦单测23/23、7文件静态分析0问题；模块搬移前后14/14章输出逐字一致。父 AOT 为
  6,667,776 bytes，候选为6,670,848 bytes（+3,072 bytes）。同机器状态、同14章并发重跑：父版本
  V4子耗时9,966.03 ms，候选10,105.89 ms（+1.4%）；中位章730.61 -> 723.31 ms。差异按负载噪声
  处理，没有算法级性能回退。早期7,046.32 ms历史快照来自不同负载，不再用于同轮判定。
- 解析器结论不变：E02 的 UDPipe 已提供 AUX/VERB 谓词链，E32 的非从句逗号分类也不等于错树；
  根因是事实消费和评分优先级。Alice＋Willows 的朗读切点不是 UD head/deprel 金标，不能直接用于
  微调 UDPipe。Stanza继续只作开发期独立对照；在没有新的完整 UD 标注、端侧包体/速度和跨平台
  验收证据前，不替换当前固定 SHA 的 UDPipe 模型。

## Clean-room V4 阶段 5：21词灵活区与短尾平衡（接受为零不支持检查点）

- 分支：`codex/splitter-clean-room-v4-tail-balance`。规则收敛为：21词仍属于可切可不切的灵活区；
  只有区间内存在不跨保护关系、无强结构风险、且切后两侧均至少8词的无标点句法边界时，才施加
  主动切分压力。没有这种舒适边界时允许保留21词；22词以上仍按更高长度压力积极切分。原文标点、
  28/25词压力、结构风险和30词硬上限的既有优先级均未放宽。
- 实现没有扫描文本词面或增加章节特例。每个原始跨度在进入单次 DAG 前，按类型化边界事实构造一张
  `Int32List` 差分表：安全边界对所有可形成8/8以上的21词区间做区间标记，线性归并后每个区间只需
  O(1) 查询。没有递归、二次句法解析或第二套求解器；短于21词的跨度不分配该表。
- 四种过宽方案均已否决并撤回：把所有21词直接降级曾在全量合并17处合理舒适切点；渐进长度公式
  全量改变18处并产生 Alice E05 `playing | against herself`；把短缺分提前在代表集改变10处并破坏
  Alice E09、Willows E09 等标点路径；按“6--7词句法片段”直接计罚虽通过长度测试，却把 Alice E09
  改成 `animals | that`、把 Willows E37 改成 `full view | practically completed`，故也撤回。
- 相对阶段4，101章、2,932个原始跨度只改变6处，逐项自行复核均支持：Alice E19 的
  `so much ... | that ...` 合为完整21词结果从句；Willows E11 的14/7共享谓语改为原文逗号后的21/11；
  Willows E26 的19/6移到原文逗号成为21/4，避免 `earth | towards him`；Willows E37 目标从
  10/7/15/6变为10/7/21；Willows E51 的6/15合为完整21词；Willows E58 的14/7合为完整21词从句。
- 已确认不支持项从阶段4的 `1 / 7897 = 0.013%` 降到
  `0 / 7892 = 0.000%`。最终句槽7,892，1词句0，超过30词0，文本去空白往返失败0；21--30词由43
  增至49，均是本轮有意保留且无舒适安全切点的21词灵活块；28--30词仍为1，最长29词；emergency
  边界由13降到12。到达0以后，后续轮次必须保持0并再改善次级指标，不能重新引入不支持切点。
- 聚焦单测27/27、7文件静态分析0问题。最终差分表二进制与先前逐间隙扫描质量版在101章的章节输出
  和原始路径均0差异；E50 非连续 `originalIndex` 也完整回放通过。回放工具同时改为按同序 List 配对
  decision/path，不再把非连续业务索引误当数组位置，也不需要强制空值解包。
- 同一回放器源码、同一14章、三轮交替 AOT 公平基准：阶段4 V4中位7,437,727 us、均值
  7,526,840 us；候选中位7,350,474 us、均值7,420,683 us，分别快约1.2%和1.4%；总回放中位
  35,557,660 -> 35,210,209 us，快约1.0%。阶段4/候选 AOT 为6,670,848/6,671,360 bytes，候选
  增512 bytes；V4四个模块合计638行。早先用非同构旧二进制测得的约6%回退已作废，不能作为结论。
- 本轮接受为 clean-room V4 首个全量零不支持检查点，但仍只存在实验工具路径，不替换生产最佳，
  不迁移文章、不重建TTS。下一阶段在保持零不支持前提下处理唯一29词块、进一步减少 emergency，
  并继续以质量、速度、体积和结构共同胜出后的一次性生产对比为最终准入条件。

## Clean-room V4 阶段 6：极长倒装句的 appos 风险分级（接受为检查点）

- 分支：`codex/splitter-clean-room-v4-long-tail`。唯一29词块为 Willows E24：
  `down the tunnel ... in a faint musical hum the sound ...`。UDPipe 把 `hum -> sound` 标为 `appos`，
  V3 同时给出 `complete_dependency_phrase_subtree` 和 `surface_modifier_head_separation`；V4 原先把后者
  一律当强风险，导致28词以上压力也无法使用 `hum | the sound ...` 这一完整倒装谓语/后置主语边界。
- 通用修正只针对“无标点＋完整 dependency phrase＋唯一 `crossing_relations:appos`”上的
  `surface_modifier_head_separation`：从强结构风险移入长度临界风险。该风险与21--24词压力同层，
  因而灵活长度仍保持不切；25词以上压力才可压过。其它 modifier/head、其它 appos、标点边界和
  所有保护关系仍沿用原强风险，不增加词面、章节或倒装句词表。
- 冻结101章全部候选中只有2个边界满足该类型组合：Willows E03 的边界位于13词舒适块，实测不变；
  Willows E24 从7/29变为7/18/11：`Then, from up above and far away, | down the tunnel ... in a faint
  musical hum | the sound of distant bells ...`。这两个匹配点均已回放，代表集其余原始路径0变化；
  因影响集合已由全量事实穷举封闭，本轮不重复无差别运行101章。
- 阶段5的 `0 / 7892 = 0.000%` 不支持项保持为0；最终句槽推导为7,893，1词句0、超过30词0、
  emergency仍为12。21--30词由49降到48，28--30词由1降到0，最长由29降到27；文本和已有句槽
  只读，未迁移文章或TTS。
- 新增两项回归：29词允许18/11，24词在同一风险下保持整句。聚焦单测29/29、7文件静态分析0问题。
  同一16章、同源码回放器、三轮交替AOT基准：阶段5/候选 V4中位8,922,149/8,913,976 us，候选
  快约0.1%；均值8,973,548/8,884,356 us，快约1.0%；总回放中位42,337,068/41,804,236 us，快约
  1.3%。两版AOT均为6,671,360 bytes，没有体积回退。
- 本轮接受为新的零不支持检查点。下一阶段只审计12个 emergency 是否存在可由现有类型事实替代的
  合法边界；若不能在保持零不支持、性能和结构不回退的情况下减少，则保留它们，不为追求计数继续
  堆叠规则。

## Clean-room V4 阶段 7：emergency 真实性审计与短尾标点（接受为非零改进检查点）

- 对阶段6的12个首选 emergency 逐项回看后，纠正此前“零不支持”的过早结论：其中7个是引语边缘、
  归属间隙或完整关系从句等受支持边界，5个确实不支持，分别是 Willows E03 两处、E30、E42 和
  E50。最近检查点的真实下限应为 `5 / 7893 = 0.063%`，不能继续把阶段5/6记为质量归零。同期用
  `ReadAloudSplitterV3.wordCount` 重算报告，阶段5/6 的21--30词块应分别为48/47，而不是早期记录的
  49/48；最终句槽、硬上限和最长块记录不受此计数纠正影响。
- 通用修正把普通 emergency 明确降为硬上限 fallback，只有 `quote_edge:*` 与
  `protected_quote_attribution_gap` 保留结构化 span 边界身份；因此24词以内不能再为了均衡主动采用
  不完整成分切点，超过30词时仍可由同一 DAG 强制使用。没有增加递归、二次 parser 调用、修复窗或
  书/章/词特例。
- 17--20词的短尾标点先做过一次过宽实验：把所有4--5词完整标点块放行后，全量改变45处，并新增
  短 attribution、结巴破折号和 `called "Moly!" | several times` 等不支持切法，故当轮立即否决。
  最终条件只允许原句末尾4--5词的标点尾块，且开缘没有强结构/残余风险，也不是右引号后的短归属；
  不能制造短前缀或中间碎片。该条件直接覆盖 `lock-keeper? | It happened this way. Toad...` 和
  `beside the driver, | and on they went again`，并由三项反例测试锁定扩散边界。
- 相对阶段6，全量15个原始跨度变化均按统一规范复核。4个原不支持切点已变为合格标点或弹性 KEEP；
  E03 的 `and although ... tightly he saw ...` 虽删除了 `had got | the basket` 错切，却仍保留25词块。
  UDPipe 把 `tightly` 错挂到后面的 `saw`，使实际安全的
  `... strapped up tightly | he saw ...` 被 `surface_adverb_attachment_separation` 强风险压住；因此本轮
  不支持项严格降到 `1 / 7894 = 0.013%`，但尚未归零。其余新增变化都是17--20词内的合格源标点，
  包括完整短引语、句号、分号、破折号或逗号包围的完整后置修饰语。
- 最终句槽7,894，1词句0、超过30词0、28--30词0、最长27词；21--30词为50，其中新增的23/24/25词
  是删除不完整 emergency 后的可审计结果；emergency由12降到7。聚焦单测31/31通过。功能代码净增
  23行，仍只在一次边界事实归一化与一次 DAG 评分内完成；AOT体积6,671,360 -> 6,671,872 bytes。
- 同一 E01/E02/E33、Willows E03/E30，三轮交替 AOT：阶段6/候选中位
  11,743,363/11,752,347 us，候选慢0.08%，属于测量噪声；均值11,762,419/11,704,042 us，候选快0.50%。
  本轮因不支持项严格下降且无显著性能/结构回退，接受为分支检查点。下一轮只处理最后一个 parser
  假附着：应让“完整从句入口＋单一 adverb attachment 告警”在25词以上成为可复核的长度临界风险，
  不得换词表、按句覆盖，也不得因此放宽21--24词灵活区。

## Clean-room V4 阶段 8：代词主语从句入口与 parser 假附着（接受为归零检查点）

- 分支：`codex/splitter-clean-room-v4-parser-resilience`。阶段7唯一不支持项来自 Willows E03：UDPipe
  把 `tightly` 错挂到后面的 `saw`，使 `... strapped up tightly | he saw ...` 同时具有完整
  `dependencyClause` 候选和错误的 `surface_adverb_attachment_separation` 强告警。直接放宽所有
  `dependencyClause + adverb` 会命中101章中的1,026个候选，范围不可接受，因此没有采用。
- 最终事实只识别通用 UD 结构：右侧以 `PRON/nsubj` 开始，依存 head 是其后4词内的 `VERB/AUX`。
  该事实与既有谓词链/并列成分风险在同一次 parser token 映射中建立，统一存入按绝对字符偏移索引的
  `DependencyGapFact`；原句求解仍只做 O(1) 查询和一次 DAG。只有候选为无标点
  `dependencyClause`、恰好只有一条强告警且该告警是 adverb attachment 时，才把它从强风险降为
  长度临界风险；21--24词灵活区仍可 KEEP，25词以上长度压力才可压过。
- 代表集与101章全量都只改变1个原始跨度：
  `8/25/20/8/9/13 -> 8/14/11/20/8/9/13`，形成
  `and although ... strapped up tightly | he saw a plate ...`。其余2,931个原始跨度逐边界0变化；
  精简前的完整主语版本与最终代词主语版本在101章也0差异。
- 不支持项从 `1 / 7894 = 0.013%` 严格降为 `0 / 7895 = 0.000%`。最终句槽7,895，1词句0、
  超过30词0、21--30词49、25--27词8、28--30词0、最长27词，emergency保持7；本轮同时删除了
  最后一个25词块。该“0”只指当前 Alice＋Willows 全量规范审计，不替代生产切分器的最终对比门禁。
- 两个新事实反例把“单一告警可降级”和“存在第二条成分告警仍保持强风险”锁死；两组聚焦测试合计
  38/38通过，4项聚焦静态分析0问题。功能代码净增49行、测试净增38行；两类依存事实合并为单一
  gap map，没有第二次 parser 扫描、递归或新求解器。AOT体积6,671,872 -> 6,671,360 bytes，减少
  512 bytes。
- 同一 E01/E02/E33、Willows E03/E30 三轮交替 AOT：阶段7/候选中位
  12,618,096/12,458,879 us，候选快1.26%；均值12,574,281/12,471,553 us，候选快0.82%。质量归零、
  长度分布、速度和AOT体积同时改善，功能增量限定在新的通用事实及其消费位置，因此接受为新的
  clean-room 检查点。仍未接入生产 Service、未迁移文章或TTS；后续任何优化必须保持0不支持，并至少
  改善速度、功能代码量、模块复杂度、emergency或长度分布之一，最后才与生产最佳做一次性全量对比。

## Clean-room V4 阶段 9：冻结生产版一次性全量对比（失败，阶段8归零结论撤回）

- 分支：`codex/splitter-clean-room-v4-production-audit`。首次按最终门禁把 tag
  `splitter-v3.7-pre-refactor-20260814` 的101章代码结果与阶段8逐原始跨度、逐边界对齐。新增独立
  Node 审计器 `tools/audit_clean_room_production_comparison.mjs`，直接读取报告内已经计算好的词数，
  不再为统计工具编译并载入整个 Dart 分句库。101章、2,932个原始跨度的章节级和原始跨度级回拼
  失败均为0，源对齐失败为0。
- 冻结 tag 在本次五份固定输入上得到6,752个最终句槽；阶段8为7,895个。两者共有1,158个原始跨度
  不同，阶段8新增2,058个边界、删除916个边界，影响全部101章。新增边界中1,990个为原文标点、
  68个为无标点边界；候选仍为1词0、超过30词0、最长27词、21--30词49、emergency 7，但2--5词块
  从冻结 tag 的267增至622。该规模证明此前只复核阶段间增量不能替代最终生产差异审计。
- 全量对比立即发现至少23个确定不支持切点：10处把通用称谓缩写切成 `Mr. | Toad/Badger/...`；
  9处把插入修饰语前的协调词留在左块形成 `and, | as/after/with/...`；另有
  `bend | about easily`、`the same thing | as ...`、`anxious not | to be left out` 和
  `the Mole | and the Water Rat` 四处紧密成分内部断裂。下限为
  `>=23 / 7895 = >=0.291%`，高于阶段7真实的 `1 / 7894`，因此阶段8的“0”只覆盖了当时已知增量，
  不能成立为两书绝对质量结论；本轮判定失败，不运行性能准入、不接入生产、不迁移文章或TTS。
- 根因是事实模型缺口而非这些专名：V4把候选对象的 `hardBlocked` 当作完整事实，但 V3 候选仍会把
  称谓缩写句点暴露为强标点；V4又遗漏了已有的 `surface_nominal_coordinator_separation` 告警，并且
  没有统一表示“协调词＋插入逗号”“否定词＋不定式”“谓词＋附着副词”和同一比较结构。下一轮只能
  在一次事实构建中补齐这些通用边界事实，并用聚焦章节先证明23处全部消失且无新增不支持切点；不得
  为 `Mr.` 后的人名、章节或整句建立结果特例，也不得把修复实现成求解后窗口重排。

## Clean-room V4 阶段 10：边界完整性事实补齐（接受为非零改进检查点）

- 分支：`codex/splitter-clean-room-v4-boundary-integrity`。阶段9的23处并非完整下限；101章全量增量
  复核又发现 Willows E10 的 `had never seen | so far`，因此最近接受解的真实已知下限更正为
  `>=24 / 7895 = >=0.304%`。本轮只增加通用事实：称谓缩写句点、协调词后的插入逗号、`not + to`
  和 `same + nominal + as` 不得成为边界；相邻副词链与带前置主语的分词并列在既有 parser token
  遍历中登记组件风险。没有书名、章节、人物名或完整句白名单，也没有求解后修复窗。
- 先做24章代表集，再做101章全量回放。与阶段8可逐原始跨度对齐的97章中只改变18个原始跨度；
  另4章因冻结输入包含而当前输入排除了 `CAROL`、章节标题或节目单标题，按相同原文精确匹配后只
  改变4个共同跨度。合计22个变化跨度全部对应上述24处已知坏边界及新发现的E10，没有扩散到其它
  原文。23个阶段9坏边界和E10旧坏边界均已消失；其中22处得到支持结果，仍有两处换成了新的不支持
  切法：E10 的 `Nature was deep | in her annual slumber`，以及E58的
  `assigned to him | without a murmur, and the animals set off`。本轮已知不支持数严格降为
  `2 / 7887 = 0.025%`，因此接受为非零改进检查点，禁止写成归零。
- 当前输入快照为101章、2,927个原始跨度、7,887个最终句槽；与阶段8的2,932/7,895差8句槽来自
  上述5个输入标题/节目单跨度，不是分句算法删除正文。1词句0、超过30词0、最长27词、21--24词42、
  25--27词9、28--30词0、emergency 7，章节和原始跨度回拼失败均为0。
- 过宽捷径“把所有 `surface_nominal_coordinator_separation` 告警升级为强风险”破坏了既有信息性
  告警回归，立即撤销；最终实现把分词并列事实并入既有组件扫描。相对阶段8，功能代码净增57行，
  测试净增81行，通用审计器为支持 V4→V4 对比净增8行；AOT体积
  6,671,360 -> 6,672,896 bytes（+1,536，+0.023%）。聚焦测试44/44通过，4文件静态分析0问题。
- 同一Willows E30、父/候选交替5轮：总回放中位2,876,682/2,845,077 us，候选快1.10%；V4自身
  中位599,371/600,661 us，候选慢0.22%，按噪声处理，未证明算法级提速也无显著回退。下一轮只从
  唯一字典序和已有边界事实修正上面两个替代切点；必须继续严格少于2，且不得再堆词面特例。实验仍
  未接入生产 Service，未迁移文章、TTS、字幕或数据库。

## Clean-room V4 阶段 11：标点事实分级与 parser 降级韧性（接受为归零检查点）

- 分支：`codex/splitter-clean-room-v4-punctuation-priority`。阶段10的已知下限再次更正：除
  `deep | in her annual slumber` 和 `him | without a murmur` 外，全量复核还发现 E10 的
  `brushed him | as it dashed past`，所以父检查点实际为 `>=3 / 7887 = >=0.038%`，不是阶段10记录的
  2处。本轮没有更改全局17维排序，也没有加入书名、章节、人物、目标词或完整句覆盖。
- 三项通用事实共同修根因：介词短语的名词中心仍回挂左侧中心词时，保护左中心与介词之间的间隙；
  V3把 `deferred_stable_right_clause` 降成 emergency 时，只有右侧紧邻的 `ADV/advmod` 与显式主语共享
  同一有限谓词或系词中心，才恢复为可用从句入口；源标点上的
  `surface_parallel_list_item_separation` 从强禁切降为弱标点告警。原文标点、8--16词舒适区、
  21--24词弹性区、30词硬上限和紧密成分保护的既有优先级均未改变。
- 过宽路径均已否决并撤回：把漏过的标点整体提前到结构风险之前，代表集即新增
  `bend | about`、`side of the | boat`、`I'd have | learnt` 和 `straw | and misery`；把所有 SCONJ
  有限从句入口一并放行，全量新增 `do | as I do`。最终只允许 `ADV/advmod + nsubj + finite/cop head`
  的相邻入口，SCONJ补语继续保留保护。第二次 token 扫描曾使V4子耗时中位约回退4.4%，也已删除；
  最终事实复用既有 token 映射和主语循环，只有命中稀有结构时才检查系词子节点。
- 25章代表集相对父检查点只改变3个原始跨度，均为目标问题。随后101章全量首次宽版只改变6处，
  发现并撤回上述 E06 SCONJ 扩散；收窄版用5个全部受影响章节逐原始跨度证明 E06 恢复，后续扫描
  合并版本与收窄质量版0差异。最终影响封闭为5处：Alice E25 删除
  `such a thing | as a drawing...`；Willows E10 得到9/23/16的逗号＋完整副词从句入口，并把另一处
  6/12改为原文逗号10/8；Willows E44由10/20/12/12/6改为原文逗号上的7/10/13/12/12/6；
  Willows E58由20/8改为原文逗号23/5。5处均为支持切法，没有其它最终输出变化。
- 已知不支持项由 `>=3 / 7887 = >=0.038%` 降为 `0 / 7887 = 0.000%`。由全量宽版统计减去已验证
  恢复的唯一E06变化，最终仍为101章、2,927个原始跨度、7,887个句槽、选中边界4,972；1词0、
  超过30词0、最长27词、21--24词44、25--27词8、28--30词0、emergency 9，章节和原始跨度回拼
  失败均为0。这里的0仍是当前 Alice＋Willows 已知规范审计，不代表已经通过生产替换门禁。
- 聚焦测试49/49、4文件静态分析0问题。相对父检查点功能代码净增53行、测试净增94行；增量集中在
  已有事实模块、边界归一化和对应反例，没有第二套求解、递归或修复窗。AOT体积
  6,672,896 -> 6,673,408 bytes（+512，约0.008%）。同一Willows E30、父/候选交替7轮受机器负载
  波动明显：总回放中位3,701,456/3,489,104 us，候选快约5.7%；V4自身中位
  811,494/728,649 us，候选快约10.2%；逐对差值中位候选慢0.6%，按噪声处理，未证明算法级提速，
  也没有可证明回退。本轮接受为独立归零检查点；仍不接入生产、不迁移文章、不重建TTS。
