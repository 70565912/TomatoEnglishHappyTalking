# 不足4词前置归并：拒绝无限累积短链

2026-09-22，基线70c48b08885e1a862506f6648c50cefb7d2d2998。

首轮候选在101章和206项回归中消除了短终态，代表章交替AOT中位数快约27%。但独立短链压力探针中，连续100个3词源单元被归成一个300词DAG，候选中位数约183ms，基线约27ms。该探针与Windows编译同时进行，不用于最终性能放行；它暴露了多余大DAG的结构问题，需修正后在无构建竞争条件下复测。

拒绝“每个后续短单元永远并入已扩大的前组”。生产归并改为：已合规的多单元组在继续吸收会越过既有16词舒适上缘、且新短单元有后邻时结束；新短单元向后归并。孤立长邻仍可吸收短单元并一次求解，不以30词阻止输入。

新增100个3词单元的结构测试，要求形成多个4–16词求解单元、事实数与DAG数等于单元数。不得通过反复求解、降低候选数或减少诊断改善速度。首轮原始样本与快照在output/short-merge-20260922/attempt1/。
