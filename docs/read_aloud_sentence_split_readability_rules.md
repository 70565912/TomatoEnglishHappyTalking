# 英文朗读分句：字幕、朗读负担与分隔结构完整性规则

> 历史实现与迁移记录。当前唯一规范是
> [`read_aloud_sentence_split_spec.md`](read_aloud_sentence_split_spec.md)。本文件中的版本阶段、迁移门禁、
> 章节审核或旧排序描述不得作为新增生产规则。
> 特别是下文曾讨论的 Alice E09/E17、Willows 舒适切分和“不完整句法边界”扩展均不进入本次重构；
> 本次重构只能复现发布行为夹具中已经确认的圆括号结果，不能借机采用这些历史建议。

日期：2026-08-12
文章级版本：`read_aloud_dp_v3` / `reviewed_dp_v3`
内部求解器：`syntax_solver_v3_7`

## 目标

本分句器生成的是字幕显示、逐段朗读、跟读和听力定位使用的 **read-aloud chunk**，不是只追求
语言学完整句的断句器。30 词是阻止异常输出的最后硬上限，不是正常目标。只要不破坏紧密句法关系，
从句、补充说明、介词短语、分词尾语以及短而完整的对话都可以成为朗读块。

V3.3 把“依存风险、最少切点”放在长度负担之前，并对“总长不超过 30、最长无标点跨度不超过
20”的原句直接 KEEP。这会让 28–30 词临界长段保持不动，也会使 `8/29` 因少一个切点而压过
`8/22/7`。V3.5 撤销这两项行为。

## 不变的硬约束

- 输出只切分、不改写原文，不跨越文章或章节边界。普通内容仍按段落求解；完整匹配引语若因诗歌、
  菜单或排版换行跨越空行，可在同一章节内合并涉及的多个段落和 parser sentence 后统一求解，
  因此段落和 parser sentence 都不能机械地切碎同一引语。合并后的内部空白折叠为一个空格，
  但单词、标点和顺序不得改变。
- 回拼必须保持原文单词、标点和顺序；仅句界处空格差异视为等价。
- 不在 parser token、多词 token、单词、缩写、数字或 clitic 内部切分。
- 单个输出块和最长无标点连续段都不得超过 30 词；20 词只是开始积极寻找安全内部切点的目标阈值。
- 不把主语与核心谓语、谓语与必要宾语、介词与宾语、连词与其内容等紧密关系作为正常切点。

## 长度触发区间

| 原句或待处理块 | 行为 |
|---|---|
| `≤16` 词 | 不因长度主动切分，原文短句保持不动 |
| `17–20` 词 | 搜索全部原文标点；存在合格标点切点时，切分方案优先于 KEEP；不启用无标点切点 |
| `21–30` 词 | 先使用合格标点；标点路径仍留下 `>20` 词长段时，继续搜索安全语法切点 |
| `>30` 词 | 必须切分；标点和语法切点均不足时才使用应急词间切点 |

搜索按完整路径反复应用，不能只切第一刀后留下 28–30 词尾段。17–20 词规则中的“有标点就尽量
切”只适用于合格节奏边界；列表逗号、说话人归属、无意义插入语边界和紧密句法断裂不得机械切开。

这里的 `20` 是目标阈值，不是验证硬上限。21–24 词无标点段必须进入“考虑内部切分”的比较，
但若所有内部切点都会拆开主谓、动宾、限定、介词补语或列表项，可以保留到 21–24；只有 30
才是候选枚举与最终验证的硬上限。实现中不得再次用同一个常量同时表达“20 词目标”和“30 词硬限”。

## 完整引语范围

- 把完整匹配的直双引号 `"..."` 或弯双引号 `“...”` 识别为对话范围；直双引号的开、关角色必须
  根据前后字符和标点语境判断，不能按奇偶次序机械交替。同形嵌套引号用栈配对；某切点同时落入
  多层引语时，以最内层完整匹配范围决定短引语硬阻断。
- 引语允许因诗歌、菜单或版式换行跨越空行，但不得跨文章或章节。未闭合开引号可以短暂携带到
  下一段继续配对；如果下一段遇到的第一个引号按语境是新的开引号，则放弃上一段未闭合状态，
  防止把后文整段误吞为一个超长引语。所有未闭合引号必须在全书审计中单列。
- 单引号和英文撇号不作为本规则的对话定界符。
- 完整引语 `≤16` 词时，所有引语内部候选都保留审计信息但标记
  `hardBlocked: inside_short_complete_quote`，不得切碎短喊话、感叹或同一角色的重复语。若输出块
  恰好覆盖这一完整短引语，1–3 词短片段处罚不适用；此豁免不得扩展到任意短尾或说话人提示语。
- `17–20` 词引语沿用普通规则：先比较引号外和右引号后的切点，内部合格标点仍可积极切分。
- `>20` 词时把引语作为一个完整待处理范围；引号外切点不足以控制朗读负担时才使用引语内部切点，
  `>30` 词仍必须在内部切分到硬上限以内。
- 右引号必须留在左块。候选审计记录 `insideQuotedSpeech`、`quoteSpanWordCount`、`quoteEdge` 以及
  阻断或降级原因；同级路径在普通 `17–20` 词压力之后，优先选择引号外边界。
- 右引号后的提示语按可读性归属：不得单独制造 `he said`、`he remarked` 一类无意义短尾；若提示语
  后还有完整动作或叙述，可在右引号后切分。识别使用通用依存谓语/主语证据，不写死说话动词、
  角色名或《柳林风声》句子。

固定回归：

1. `"Onion-sauce! Onion-sauce!" | he remarked jeeringly, and was gone ...`
2. `working busily ... "Up we go! Up we go!" | till at last, pop! his snout ...`

## 合格朗读边界

朗读块不要求都是独立完整句，但必须是可理解、可连续朗读的语义或节奏单元。允许时间、条件、
原因等从句，连贯的描述性补充语，分词尾语，以及完整的短对话或感叹。下列边界不合格：

- 主语与核心谓语之间；
- 谓语与必要宾语之间；
- 介词、限定词、助动词、系词、标记词、并列连词与其紧密成分之间；
- 先行词与限制性关系从句之间；
- 只产生连接词、说话人提示或其它没有独立意义的 1–5 词碎片。切分新制造的 1–3 词片段一律
  按高风险尾巴处理，即使恰好落在句号、问号或感叹号前；4–5 词块若以完整句号、问号、感叹号、
  分号或冒号收束，且 UDPipe 能确认其中有主谓结构或独立祈使谓语，才可作为完整短句或短对话
  豁免。原文自身就是短句的情况不受此限制。

完整原文标点都必须先进入候选识别，不得只保留 parser 已确认的从句逗号。候选按强标点、从句
逗号、短语逗号、歧义逗号分级；歧义逗号保留在候选集中，但不能仅为了把 17–20 词降到 16 词
以下而压过 KEEP。若它能消除 21–30 词长段，则可在没有句法硬伤时参与全路径比较。
破折号、分号、冒号或逗号即使与后一个单词之间没有空格，也必须按“左块保留标点、右块从下一
单词开始”计成两个词法单元和一个候选边界。UDPipe 可能把 `—if`、`—he` 解析为一个带前置标点
的 token；此时标点后的切点不是“切进单词”，不得触发 `inside_parser_token` 硬阻断。纯标点 token
不占单词预算，但必须完整附着到相邻输出，确保回拼不漏标点。

## 路径排序

路径采用严格字典序，不使用一个可互相抵消的总积分。顺序为：

1. 新制造的 1–3 词尾巴，以及没有完整停顿的 4–5 词碎片；
2. `28–30` 词临界长段数量；
3. `25–27` 词严重长段数量；
4. 先行词/物主结构等通用表面句法警告；
5. 无原文标点切点的受保护依存关系 crossing，包括主谓、动宾、限定、介词标记和数词修饰关系；
6. `21–24` 词明显长段数量；
7. 标点类型和其它依存关系构成的边界自然度；
8. 原文标点切点的受保护依存 crossing；它仍触发复核，但不能仅凭一次 UDPipe 错树压过
   21–24 词段内的自然标点；
9. `17–20` 词可继续按合格标点切分的长段数量；
10. 引语内部切点数量；短引语内部切点此前已被硬阻断，此项只在其它长度与结构条件同级时让
    引号外方案优先；
11. 其它表面软警告、最长块、全路径阅读负担、短片段和 8–16 词偏离；
12. 切分次数；
13. 长度均衡。

`hardBlocked` 只用于 parser token、单词等确定不能切的位置。UDPipe 的 protected crossing 仍是软证据，
不能因一次错树让 `1/30/2` 或 `20/4/4/12` 压过可读路径，也不能让 25 词 KEEP 压过自然的
`13/12`；但 21–24 词仅表示“开始考虑内部切分”，不能为了机械降到 20 以内而选择 `pail / of
whitewash`、`outer world / of sunshine`、`he / flung` 一类破坏紧密依存关系的切点。最终路径若
含 protected crossing、歧义逗号、表面软警告或无标点切点，必须进入受约束 AI 或人工复核。

表面结构警告用于补偿单次 UDPipe 错树或词性误判，当前至少覆盖：谓语、代词、限定词等与后接
介词短语，`noun / of` 所有格补语，介词与右侧宾语，名词与后接并列名词，`all / three` 一类
数量结构，谓语与后接所有格宾语，`where / they` 一类关系标记与主语，显式主谓/动宾关系，
`to / verb`、`as / if` 等固定连接结构，以及 `best-guarded / keep` 一类修饰语与中心名词。
标点判断只检查切点左侧结尾的标点，不能把同一个 source word 前部的破折号误当成当前切点停顿。
这些规则参与通用排序，不写死《柳林风声》的章节号或完整句子。

因此，两个路径都没有受保护句法断裂时，`8/22/7` 必须压过 `8/29`；不能因为多切一刀而保留
接近 30 词的字幕。切分次数只用于同级方案的末端择优。

## E61 固定回归

原句：

> When the other animals came back to luncheon, very boisterous and breezy after a morning on the river, the Mole, whose conscience had been pricking him, looked doubtfully at Toad, expecting to find him sulky or depressed.

最低要求是 `8/22/7` 优于 `8/29`。完整标点和句法候选可用时，本地首选应为：

1. `When the other animals came back to luncheon,`（8）
2. `very boisterous and breezy after a morning on the river,`（10）
3. `the Mole, whose conscience had been pricking him, looked doubtfully at Toad,`（12）
4. `expecting to find him sulky or depressed.`（7）

第 20 词和第 26 词后的逗号分别会形成过短名词碎片或拆开主语与谓语，不应选择。

## 全书迁移验收

- 动态规划对任意后缀状态评分时，长度和短片段必须从该状态的 `startWord` 计算；不得把后缀
  `19/8` 误算成 `32/8` 后在 beam 剪枝阶段丢掉完整正确路径。候选召回是排序生效的前提。
- 旧 243 项金标中的“安全原句 KEEP”标签是 V3.3 历史策略，必须按本规则重新分类，不能为了保持
  旧通过率而继续接受 28–30 词长段。
- 全书重跑后单列所有 21–30 词残留块、1–7 词新增块、歧义逗号路径、非标点路径和应急路径，
  逐项复核后才冻结英文分句。
- 分句冻结后再处理中文：边界未变且译文合适的句子复用；边界变化的句子按童话叙事风格重译，
  `Mole` / `Toad` / `Badger` 等既有称呼保持一致。
- 组图映射按原文字符覆盖重新对齐到新的持久化句子槽；本阶段不重新合成语音。
- 全书迁移、翻译、E61 原文修复和组图重映射只能由独立 `tools/` 脚本或临时工作目录完成，
  不得增加或保留正式 App Bridge、Service、Provider、Screen 等迁移入口，也不得打包进 Release。

## 本次一次性实施边界

本次《柳林风声》重建使用下列仓库外层工具，正式 Flutter/Web 运行代码不提供迁移命令：

- `app/tool/split_willows_sentences.dart`：以同一 UDPipe 模型运行全书当前求解器审计，生成候选、首选路径和
  风险报告；可直接切片复用完整全文 UDPipe 缓存，按章独立调用正式求解器，确保未闭合引号不会跨章
  传播。它只读原书工作目录，不写产品数据库。
- `tools/remap_willows_parsed_cache_to_current_db.py`：只读核对数据库当前英文正文与历史全文缓存，
  只允许删除已确认重复或旧排版标题后重映射 token/offset，不运行 UDPipe。
- `tools/merge_willows_split_reports.mjs`：合并逐章只读报告并生成新旧分句差异。
- `tools/audit_willows_quote_split_v3_6.py`：独立复核引号配对、短引语内部切点、规范化回拼和本地音频
  可复用状态；不迁移数据库，不调用 TTS、ASR 或 AI。
- `tools/prepare_willows_v3_offline_review.mjs`：把冻结英文与历史 V2 中英句槽按规范化字符区间对齐，
  仅把完全相同的旧句列为“可检查复用”，边界变化一律列为“重译”。
- `tools/finalize_willows_offline_review.mjs`：要求每个句槽都明确标为 `reused_checked` 或
  `retranslated`，并检查 30 词上限、英文回拼、空译文和固定英文角色名；不调用翻译 API。
- `tools/migrate_willows_segmentation_v3_offline.py`：直接离线读写 SQLite，默认只做只读 dry-run。
  `--apply` 前必须已有完整终稿；执行时先生成 SQLite 备份，再在一个 `BEGIN IMMEDIATE` 事务内替换
  句槽、译文与组图句子范围，回读核对后才提交。脚本只允许 E61 删除已确认且哈希固定的 373 字符
  重复段，以及 E35 删除已对照权威原书、哈希固定的 123 个数据库规范化字符重复段；其它章节不得
  删除、替换或插入任何英文单词或数字。原书与历史数据库之间已经核实的纯标点规范化差异允许迁移，
  但必须逐项写入日志；图片路径、缓存键和状态必须保持原样。

该迁移不扫描、删除或重建 TTS/录音/图片缓存，不启动 App，不调用 Bridge，也不产生任何远程 API
费用。只有全部 62 章人工翻译终稿、全书风险复核和 dry-run 同时通过后，才允许使用 `--apply`；
未满足条件时不得部分更新正式数据库。

## 2026-08-11 V3.6 全书只读审核结果

本轮已完成正式求解器、测试与全书只读复算，当前停在英文分句人工审核闸门；以下结果不是数据库
迁移结果，也不是最终 TTS 费用结算：

- 输入为当前数据库 62 章英文正文，合并正文 SHA-256 为
  `9aaf0e83ba3b70491b7574f00ea09ac16847d4beff7f8bf154aae0411b0ae358`；复用 UDPipe 1.4.0
  全文缓存，模型 SHA-256 为 `b71fb73473bedbca575bfc927fceb0f6dd53f74493bb9c58a9e77bd28d24a71f`。
  62 章解析健康，`nativeCalls=0`。
- 旧 4,995 个句槽只读重算为 4,673 个；59 章发生变化，共 395 个最小变化区间，需逐项人工审核。
- 400 个完整匹配引语范围含现有或新边界；`≤16` 词完整引语内部已选切点为 0。另有 8 处未闭合
  引号，涉及 E34、E44、E50、E52、E53，已在专项报告单列，主要是诗歌分节或原书排版约定。
- E01 固定得到 `"Onion-sauce! Onion-sauce!" | he remarked ...` 和
  `working ... "Up we go! Up we go!" | till at last ...`；E61 固定回归为 `8/10/12/7`。
- 初步本地音频评估为 `reusable=4179`（其中 `speech-sequence=16`）、`synthesis=494`，待合成
  32,959 个字符。该数字必须在英文、翻译和组图映射冻结后再次计算；本轮远程调用为 0。
- 翻译、782 页组图映射、数据库、TTS、视频和网盘均未修改。只有 395 个变化区间全部人工通过后，
  才进入翻译和图片重映射并生成第二次迁移前报告。
- 人工审核入口为
  `output/sentence-split-v3/willows-v3-6-final-manual-review-2026-08-11/manual-review/README.md`；
  完整机器报告为同目录 `willows-v3-6-quote-diff-audit.json`，逐章合并报告为上级目录
  `willows-v3-report.json`。
- 2026-08-11 项目所有者已逐项确认 395/395 个变化区间通过，英文分句正式冻结，允许进入离线译文
  和组图重映射阶段；在第二次迁移前报告再次确认前，数据库、TTS 和视频仍不得修改。
- E02 连写食品清单已由用户人工试听确认火山 Seed TTS 2.0 + Dacey 能直接正确处理，作为未来语音
  模型选择的专项回归归档在 `docs/archive/tts_concatenated_food_list_model_evaluation_20260811.md`；
  该结论不授权改写原书字幕，也不自动外推到其它模型或音色。

为避免极长引语导致候选覆盖审计成倍重跑，正式实现保留所有边界候选及其审计字段，但完整展示路径
最多 24 条；候选超过 90 个的单元不再为每个边界额外强制重跑覆盖探针，而使用轻量可行性预检和
有界 beam。该优化不删除边界候选，不改变 30 词硬上限，也不绕过全局基础动态规划。

## 2026-08-10 V3.5 全书历史实施结果

以下仅是 V3.5 已迁移快照，不代表 V3.6 引语修复已经迁移。V3.6 必须复用全文 UDPipe 缓存先生成
全书差异和引语专项报告，待所有变化人工逐项通过后才可处理翻译、组图映射、TTS 和视频。

- 直接复用此前已经跑完全文的 UDPipe 解析结果；删除 E35 已确认重复段后，仅在既有 token/offset
  基线上重映射出
  `output/sentence-split-v3/willows-v3-5-parsed-baseline-e35-dedup-2026-08-10.json`。最终四组重评分报告
  `output/sentence-split-v3/willows-v3-5-final-audit-v6-2026-08-10/willows-v3-report.json` 的
  `nativeCalls=0`，没有再次运行 UDPipe，也没有调用分句 AI。
- E01-E62 共 62 章全部通过 V3.5 审计，最终 4,995 个句槽；最长 24 词，`≤16` 词 4,079 条、
  `17–20` 词 904 条、`21–24` 词 12 条、`25+` 词 0 条。E61 固定回归实际为 `8/10/12/7`。
- 中文终稿与 4,995 个英文句槽一一对应：843 条旧译文经检查后复用，4,152 条因边界变化重新翻译。
  翻译过程未调用 API，并完成 `Mole`、`Rat`、`Toad`、`Badger` 等称呼及异常名称空格检查。
- E35 和 E61 的重复原文均按固定哈希删除。E35 的数据库实际删除范围是 123 个规范化字符，
  SHA-256 为 `925dd69fb53059ff5c7ed6000ec4c44f2b82f655f9fcf1c896713775a40b351f`；E61 是
  373 个规范化字符，SHA-256 为 `ab82fd9bdb0f325fce6d00fb4a0846301f7e64ee0188fc3b82a619430bfd91e7`。
- 正式 Release 数据库从 3,775 个旧句槽迁移到 4,995 个新句槽；782 个绘本页的句子范围全部重对齐，
  页数、图片路径、图片缓存键、生成状态和媒体指纹保持不变。边界对齐累计移动 1,995 个规范化字符，
  单页最大移动 93 个字符（E61 删除重复后的第 6 页附近），这不是图片或正文内容移动。
- 迁移前 dry-run、事务内 apply 后回读、独立新旧数据库字段比对、迁移后幂等 dry-run 均通过；幂等检查
  再次得到 4,995/4,995 个中英文句槽、782 页、0 个边界移动、0 个待删除字符。备份及完整逐章日志位于
  `output/sentence-split-v3/willows-v3-5-migration-2026-08-10/`。
- 本次未重新合成 TTS，未改录音、音频、图片或其它书籍记录，远程 API 调用数为 0。

## V3.7 规则（syntax_solver_v3_7）

> **实现入口**：`app/lib/services/read_aloud_splitter_v3.dart`（`solverVersion = syntax_solver_v3_7`）；回归 `app/test/read_aloud_splitter_v3_test.dart`。
>
> **影响面收窄**：括号长度规则只作用于已配对 `(...)` 的 span 及其开闭缘。生产求解器只在当前原句的最小结构窗内求解；旧文章重建则以显式加载的已审核 V3.7 句槽为稳定骨架，括号内部修复不得重排后文。通用句法闭合修复也只能替换缺陷切点，不得改动窗内其它已审核边界。1 词语块在 `plan()` 后置合并（正式分句方案，非导出特例）。

### V3.7 冻结候选规范（2026-08-13）

本节是 V3.7 代码修改的唯一判定顺序。它先于后面的实现说明；如旧文字与本节冲突，以本节为准。
当前状态为“冻结候选”，只有目标回归、全书差异审核和零 `unexpected` 同时通过后，才可改为“已冻结”。

#### 1. 不变量与长度分级

1. 只插入边界，绝不增删、替换或移动原文字符及标点；规范化空白后必须完整回拼。
2. 30 词是输出块和最长无标点跨度的硬上限；任何路径超过 30 都不可选。
3. 20 词是积极切分阈值，不是硬上限。每次选择边界后，必须对每个结果块重新应用长度分级，不能因原句中已经存在冒号、分号或句点就停止处理其前方仍然 `>20` 的块。
4. `≤16` 词不因长度主动切；`17–20` 只比较合格原文标点；`21–30` 先比较合格原文标点，再比较安全语法边界；`>30` 必须切。
5. 完整路径先消除 `28–30`、再消除 `25–27`、再消除 `21–24` 词块。合法的 `16/7` 优于保留 `23`；不能用“少切一刀”覆盖朗读舒适度。

#### 2. 边界的句法闭合判定

边界不是按“左右都必须是完整句子”机械判断，而按右侧是否仍带有可恢复的结构入口判断。

- **禁止的裸依附片段**：右块以失去左侧支配词便无法独立解释的补语、谓语或短语核心开头，且边界处没有显式连接标记。例如谓语与裸 `xcomp`、主语与其延后谓语、动词与小品词、限定词与中心词之间。`seemed | quite natural`、`stopped | rowing ...`、`the Mole | to his surprise ... found ...`、`... full view | practically completed ...` 属于此类，候选保留审计信息但不得进入可选路径。
- **允许的显式续接片段**：右块以原文中的并列连词、从属连词、关系词、介词或比较标记明确开头，并形成可理解的并列项、从句、介词短语或比较结构。`birds | and animals that ...`、`hot | for the ...`、`appear | as much as possible ...`、`sovereigns | that Toad ... practically completed ...` 可以参与路径比较；不得因它们不是独立完整句而一律禁止。
- 显式入口只解除“裸依附”禁令，不自动使边界胜出；仍须比较长度、原文标点、短片段自然度和稳定性。
- 不得按 `quite`、`natural`、`hot`、`appear` 等具体单词做例外。实现只能使用词性、依存拓扑、边界首词类别和原文标点。

#### 3. 短片段

1. 1 词块一律按后置局部合并规则消除。
2. 2–5 词普通残片仍是高惩罚，但以下“完整朗读功能块”不按无意义碎片处罚：
   - 完整短引语、感叹或称呼；
   - 位于原句/分号后分句开头、以原文逗号结束的完整前置修饰语，且后块具有显式主谓骨架；
   - 带显式连接标记的并列项或从属续接，并且它能消除 `>20` 词块。
3. 短功能块是允许项，不是普遍优先项。只有它修复不合法边界、保留更强原文标点或消除 `>20` 词块时才能胜出。

#### 4. 引语与括号

1. 引语和括号共同使用 DelimitedSpan 长度分级与 30 词硬上限；两者都允许在长 span 内切分。
2. QuoteSpan 独有说话归属规则；ParentheticalSpan 不复制 `he said` 等引语语义。
3. 闭引号后的 `and + 显式主语 + 谓语` 是新的协调分句，可在闭引号后切；没有自己主语的提示语或归属尾巴继续附着引语。
4. 配对括号内部像配对引语一样先合并 parser sentence，再在 span 内求解；短 span 保护和长 span 切分都不得越过匹配范围扩散。

#### 5. 最小影响与稳定路径

1. **已审核 V3.7 的最终句槽边界**是跨版本影响审计与迁移的稳定骨架；它不是把某些新诊断关闭后重新跑一次 DP 得到的“近似基线”。新规则只能在下列局部触发：已选边界被判为“裸依附”；某个已选块仍 `>20` 且存在更舒适的合法边界；配对括号 span；1 词后置合并；协调引语闭缘。
2. 修复窗口严格取触发边界相邻的两个既有稳定边界；多个触发窗口只有相交时才合并。窗口外的 path ID 和边界必须逐一相同。
3. 一个窗口只能替换造成问题的边界，不得借句法重评重新排序窗口内所有原本合规的边界。裸依附修复只允许一个已审核缺陷切点与一个合法新切点做 `1→1` 原子替换；舒适切分只允许在一个已审核 `>20` 词句槽内纯新增切点。若同一稳定窗出现 `2→1`、`1→2`、移动其它切点或只有把候选与基线拼成不存在的混合路径才能通过，则整窗记为 `unexpected`。若没有同时满足回拼、30 词和闭合条件的替代路径，本次求解失败并进入人工审核；不得把含被降级切点的稳定路径重新放回可选集，也不得向后级联。
4. 全书差异按“旧 V3.7 → 当前候选”计算；每个变化必须能归入上述触发类型。任一非目标类型或 `unexpected` 均阻止冻结、迁移和 TTS。
5. 生产求解器可生成新文章的首选路径，但不得仅凭当前评分重算去替代历史已审核句槽；旧文章重建必须显式加载已审核 V3.7 基线，将其边界映射到当前候选后做局部修复。基线边界若因新诊断被降级，可替换；未命中批准触发的基线边界必须逐一保留。若基线不是当前候选中的可验证路径，则失败进入人工审核，不得整窗回退重排。

#### 6. 本轮固定回归

- Alice E09：`It was high time ... with the birds | and animals that had fallen into it:`（16/7）；冒号不能让其前面的 23 词块停止继续求解。
- Willows E01：`"Now then, step lively!" | and the Mole ...`（4/19）；不得保留 `the Mole | to his surprise ... found ...`。
- Willows E09：保持已审核 V3.7 的 15/11，不修改原文标点。
- Willows E30：保持 `... sufficiently hot | for the incorrigible ...`；介词 `for` 是显式续接入口。
- Willows E32：`Breathless and transfixed, | the Mole stopped rowing ... wave, | caught him up ...`（3/18/7）；原文逗号前的三词前置修饰语是完整朗读功能块。
- Willows E37 第一处：`... interview, | and the sight of certain gold sovereigns | that Toad ... discuss.`（10/7/21）；禁止 `full view | practically completed ...` 裸谓语尾。
- Willows E37 第二处：保持 `... make her aunt appear | as much as possible ...`（12/14）；比较标记 `as` 是显式续接入口。

#### 7. 性能与复杂度冻结约束

性能是分句规则可交付性的一部分，但任何加速都不得改变候选集合、候选顺序、评分键、首选路径或最终句槽。优化前后必须对 Alice 与 Willows 全书比较 `v3LocalSentences`、`localPathId`、`boundaryCandidates`、`candidatePaths`（含顺序）及规范化报告摘要；任一差异均视为算法变化，不能以“只是性能优化”放行。

1. 规则增加必须先归并为一次性的结构事实或评分特征，不得为每条新规则再套一层完整候选构建、完整 DP 或递归重跑。禁止形成“发现一个诊断 → 重跑全窗 → 再发现一个诊断 → 再重跑”的无界链路。
2. 允许的递归仅限词下标单调前进、状态数有明确上界且带 memo 的有向无环 DP；同一状态与同一评分上下文只能求值一次。不得按候选边界逐个启动彼此独立的完整 DP 来补审核覆盖。
3. 每个原句的分词、依存映射、边界 crossing、词位结构特征和区间朗读指标应计算一次并在该原句内复用。current / pre-closure / parenthetical 等候选视图应从同一份结构事实派生，不能重复扫描整棵依存树。
4. `(startWord, endWord)` 的规范化文本、无标点跨度、短片段性质以及完整 draft score 可以按“原句 + 全部评分开关”缓存。缓存不得遗漏影响评分的 flag，也不得跨正文、parser/model 或 solver version 复用。
5. `maxCandidatePaths=8`、`maxExpandedCandidatePaths=24` 是输出/审核上限，不是允许重复运行 8 次或 24 次完整 DP 的计算预算。覆盖查询应共享同一个候选 DAG、区间指标与评分缓存；如尚未完成共享实现，必须保持严格有界并记录实际调用次数。
6. 不得通过缩小 beam、减少审核候选、跳过括号/引语内部求解、提前接受首个可行路径或关闭句法诊断提速；这些都会改变算法语义。章节并行、解析/计划指纹缓存、索引、memo、对象复用和等价的多查询 DP 可以采用。
7. 性能改动必须记录 AOT 同机基准，至少覆盖普通短句、Alice E02 长括号和 Alice E30 长引语/多候选章；同时保留阶段耗时（候选构建、基础 DP、覆盖查询、闭合修复）。没有输出等价证据和基准证据，不得宣称优化完成。

### DelimitedSpan（引语 / 括号共用长度分级）

| 词数 | 行为 |
|------|------|
| ≤16 | span 内部切点全部 `hardBlocked`，整体保留 |
| 17–20 | 普通标点规则 |
| 21–30 | 优先 span 内标点与**闭缘**（引号右缘 / `);` `),` `).`）；不足再用安全句法 |
| >30 | 必须在 span 内切；30 词硬上限不变 |
| 未闭合 | 不应用 span 保护；按 offset 审计告警；段落边界清空匹配栈，不跨段猜测配对 |

**QuoteSpan 专属（括号不照搬）**：说话归属、`he said` 提示语短尾、相邻引语归属、短喊话对话轮次语义。

**ParentheticalSpan**：栈匹配可嵌套 `()`；记录起止、词数、嵌套层、edge（内部 / 闭缘后）。

完整括号内部如果被 UDPipe 切成多个 orthographic sentence，必须像完整引语一样先合并这些 parser sentence，再在同一个 span 中应用上述长度分级。

### 括号范围内的求解顺序

1. 全量提取原文标点和依存候选；短完整 span 内部候选直接硬阻断。
2. 句首长括号只重算 `[句首, 右括号]`，强制保留右括号边界；生产求解器的右括号之后不受 ParentheticalSpan 加权影响。旧文章重建时，右括号之后逐一复用已审核 V3.7 边界，禁止括号修复向后扩散。
3. `dependencyClause` / `dependencyPhrase` 候选的闭合判断是通用结构规则：只检查**跨越当前候选边界**的依存弧。`xcomp`、主谓、动宾、限定、介词宾语等未闭合结构降为 `emergency`；不得因更后方存在另一完整主谓句而放行当前边界。生产求解器可在当前原句的最小局部窗口形成新文章首选；旧文章重建不得把内部重算路径当作历史稳定骨架，必须显式加载已审核 V3.7 句槽并按 §5 的原子变化门禁处理。被降级边界继续保留在 `boundaryCandidates` 供审计，但包含它的完整路径不得进入可供本地首选或 AI 选择的 `candidatePaths`；过滤后 initial/expanded 必须共享同一有效 path ID。
4. `17–20` 词括号压力只按每个候选段与 ParentheticalSpan 的实际重叠词数计分；句法闭合和受保护依存 crossing 优先于括号长度偏好，防止产生 `running | out`、`make | out`；无括号句该括号分值恒为 0，保持原有排序。

固定回归：Alice E02 → `6/12/9` + `);` 后外句；禁止 `seemed | quite natural`。

### 1 词语块后置合并

- `plan()` / `applySelectedPathIds` 完成后，对每个英文词数 == 1 的块只看局部窗 `[prev, one, next]`：
  1. 非文首时优先并入上一块（结果 ≤30 词则采用）；
  2. 否则并入下一块（≤30）；
  3. 两侧都已 30 词：不重新运行全窗口 DP；`30/1` 只移动紧邻一词成为 `29/2`，`30/1/30` 成为 `29/2/30`。**窗外边界不动，也不得级联搬移后续词**。
- 出口不变量：最终无 1 词语块（全文仅 1 词除外）；每块 ≤30；生产校验与 `plan()` 共用 `requiredBoundaryWordOffsetsAfterMerge`（合并后仍存活的原解析边界）。
- 旧文章不会自动重分。其听力 SRT 是趣配音兼容产物，导出时可再做相邻 cue 兜底：优先并入前一条，前条已 30 词才并入后一条；只改导出的 SRT 文本与该 cue 时间范围，不写回 `articles.sentences`，不改变音频、烧录字幕、歌曲 SRT 或更后面的 cue。无法安全吸收的历史 `30/1/30` 仍须显式重建文章，导出层不得伪造词级时间或中译对齐。
- 固定回归：Willows E21 `Home!`、E46 `Free!`、E50 `No.`；`30词. + Home! + Next…` 不得留下 singleton。
- 迁移时此类变更默认 `audioStrategy=concat_adjacent_cache`（不调云 TTS）；括号类变更为 `resynth_if_missing`。

### 历史问题对照（已由 V3.7 覆盖）

| 原记账 | 根因摘要 | V3.7 处置 |
|--------|----------|----------|
| A 单词语块 | ≤16 整句 KEEP、无跨句合并 | 后置合并 |
| B E02 括号 | 无 paren span；过宽 `dependencyClause`；syntax fallback 替换可行标点 | ParentheticalSpan + 含括号句骨架优先 + 闭合门闩 |

改算法后须显式重建受影响文章并重对齐听力/字幕/译文/绘本。迁移前运行：

```powershell
python tools/audit_split_v3_7_impact.py `
  --baseline <reviewed-v3.7-report.json> `
  --candidate <v3.7-report.json> `
  --output <impact-audit.json>
```

审计基线必须是已审核的旧 V3.7 报告，不是 V3.6。审计器以新旧 V3.7 共同保留的边界作为稳定锚点，逐边界只允许 `one_word_local_merge`、`parenthetical_span`、`incomplete_constituent`、`over_20_comfort_split`、`coordinated_quote_edge` 五类最小变化；其中 `incomplete_constituent` 必须是上述 `1→1` 原子替换，`over_20_comfort_split` 必须是旧句槽内唯一的纯新增切点。审计输出的 `gatedSentences` 仅用于展示和估算，不是迁移输入；任何 `unexpected` 直接非零退出，`migrationAllowed=false`，不得 migrate 或生成 TTS。
